%li{ id: feed_item.id }
	= link_to(gravatar_for(feed_item.user, size: 65), feed_item.user)
	%span.user= link_to(feed_item.user.name, feed_item.user)
	%span.username= "\"#{feed_item.user.username}\""
	%span.content= wrap(feed_item.content)
	%span.timestamp= "Posted #{time_ago_in_words(feed_item.created_at)} ago."
	- if current_user?(feed_item.user)
		= render 'shared/delete_micropost', micropost: feed_item

